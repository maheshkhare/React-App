function App() {
  let title = "Map Demo"; //varible
  let user = { id: 1, city: "mumbai" }; //object

  return (
    <div>
      <h1>{title}</h1>
      <h1>{user.city}</h1>
    </div>
  );
}

export default App;

//user.city........this function will mapping the values
